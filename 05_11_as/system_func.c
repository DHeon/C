#include <stdio.h>
#include <stdlib.h>

int main()
{
    system("dir"); //system 함수는 명령 프롬프트의 명령을 실행시키는 함수이다.
    //dir은 경로의 파일과 폴더의 목록이 뜬다.
    
    system("cls");
    //cls는 출력 화면을 지운다.
    return 0;
}
